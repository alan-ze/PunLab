[project]
name = "punlab-monorepo"
version = "0.1.0"
description = "Shared environment for all PunLab projects"
authors = [
    {name = "Alan Zeng"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
